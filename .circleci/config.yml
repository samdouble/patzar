version: 2.1

jobs:
  build:
    docker:
      - image: cimg/rust:1.57.0
    steps:
      - checkout
      - run:
          name: Build
          command: |
            cargo build
            cargo test
